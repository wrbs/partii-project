;; Adapted from OCamlPro's ocamlbench-repo
;; See https://github.com/OCamlPro/ocamlbench-repo
(executable
 (name sequence_cps)
 (modes byte exe))

(alias (name buildbench) (deps sequence_cps.exe))
(alias (name buildbench_byte) (deps sequence_cps.bc))
