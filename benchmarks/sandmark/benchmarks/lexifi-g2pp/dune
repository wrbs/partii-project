;; Adapted from OCamlPro's ocamlbench-repo
;; See https://github.com/OCamlPro/ocamlbench-repo
(executable
 (name main)
 (modes byte exe))

(alias (name buildbench) (deps main.exe))
(alias (name buildbench_byte) (deps main.bc))
